apiVersion: kustomize.config.k8s.io/v1
kind: Kustomization

# Base manifests
bases:
  - ../../base

# Patches for production
patchesStrategicMerge:
  - deployment-patch.yaml

# No local DB in production; services connect to external RDS/DB
